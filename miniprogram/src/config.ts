// ============================================================
// 后端地址配置
// ------------------------------------------------------------
// API_BASE 三种用法：
//   1. 本地联调（默认）："http://localhost:3000" —— 指向本仓库的 Next.js 后端。
//      需在「微信开发者工具 → 右上角 详情 → 本地设置」勾选
//      “不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书”。（仅开发期有效）
//   2. 线上：填你的 HTTPS 域名，如 "https://citypulse.example.com"，
//      并在 https://mp.weixin.qq.com → 开发管理 → 开发设置 → 服务器域名，
//      把该域名加入「request 合法域名」白名单（真机/发布必须）。
//   3. 留空 ""：使用内置 mock 数据，无需后端即可预览。
// ============================================================
export const API_BASE = "http://localhost:3000";

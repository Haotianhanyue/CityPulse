- **核心架构**：采用 `TestHarness` 类封装测试生命周期，包含加载 HTML、执行断言、生成报告三个阶段。
- **模块化测试集**：内部划分为五个独立测试套件（设计系统、页面结构、响应式设计、可访问性、交互功能），通过 `--suite` 参数按需执行。
- **数据与逻辑分离**：`fixtures.js` 集中管理设计 Token（颜色、字体、间距）和预期页面结构，`run-tests.js` 负责解析 HTML 并比对数据。
- **输出机制**：测试结果实时打印至控制台，并最终序列化为 `test-report.json` 文件供后续分析。